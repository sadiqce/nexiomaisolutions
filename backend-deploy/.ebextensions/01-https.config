option_settings:
  # Node.js environment configuration
  aws:elasticbeanstalk:application:environment:
    NODE_ENV: production
  
  # Elastic Beanstalk health check configuration
  aws:elasticbeanstalk:environment:process:default:
    HealthCheckPath: /health
    HealthCheckInterval: 30
    HealthCheckTimeout: 5
    HealthyThresholdCount: 3
    UnhealthyThresholdCount: 5
  
  # Configure load balancer to accept both HTTP and HTTPS
  aws:elbv2:listener:default:
    Protocol: HTTP
  
  aws:elbv2:listener:443:
    Protocol: HTTPS

files:
  "/opt/elasticbeanstalk/tasks/bundlelogs.d/01_nodejs.conf":
    mode: "000755"
    owner: root
    group: root
    content: |
      /var/log/nodejs/*

commands:
  01_install_https_dependencies:
    command: |
      cd /var/app/current && npm install selfsigned 2>&1 || echo "selfsigned already installed"
    ignoreErrors: true
